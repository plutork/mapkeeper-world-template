# Map assets

Place map image(s) and layer files here (optional — V0 starts from a blank map).

Cell grid: hex, axial `q`/`r` coordinates, manual paint in the editor. Cell address = `cell_id` — see [`profiles/README.md`](../profiles/README.md).
